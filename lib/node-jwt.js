var node_jwt = exports;

// Export the Base64URL module.
node_jwt.base64url = require('base64url');

// Export the HMAC module.
node_jwt.hmac = require('hmac');

// Export the JWS module.
node_jwt.jws = require('jws');

// Export the JWT module.
node_jwt.jwt = require('jwt');

// Export the public key module.
node_jwt.pubkey = require('pubkey');
